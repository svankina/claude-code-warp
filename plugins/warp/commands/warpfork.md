---
description: Fork this Claude Code session into a new Warp window
argument-hint: "[session-id]  (default: current session)"
---

# /warpfork

Run this and report the output:

```bash
~/.local/bin/warpfork $ARGUMENTS
```
